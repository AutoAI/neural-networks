#from yo boiii Sid
import random as r


#Nodes, that can have an array of other nodes sai

class Node:
    def __init__(inList):
        self.coefflist = []
        self.input = inList
        self.val = 0
        i = 0
        while i < len(inList):
            self.coefflist.append(r.random()) # random element
            i+=1
    def firetrain(ac):
        co = 0
        i = 0
        while i < len(self.input):
            co += self.coefflist[i]*(self.input[i].val)
            i+=1


        if co > 1:
            self.val = float(1)

        if self
        
    
    def firetest():
        co = 0
        i = 0
        while i < len(self.input):
            co += self.coefflist[i]*(self.input[i].val)
            i+=1


        if co > 1:
            self.val = float(1)




        
